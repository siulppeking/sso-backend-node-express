# Npm configuration for the project

# Use exact versions for reproducibility
save-exact=true

# Limit audit to production dependencies
audit-level=moderate

# Disable fund messages
fund=false

# Use legacy peer dependency handling
legacy-peer-deps=false
