# Production Environment Variables Example
# 
# Use this file as a reference for production deployments.
# NEVER commit .env or .env.production to version control.
# 
# Copy and adapt these values to your production environment.

# ==================== CRITICAL ====================
# Change these values in production!

MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/sso_db_production
PORT=4000
NODE_ENV=production

# Generate with: bash scripts/generate-secrets.sh
JWT_SECRET=<GENERATE-WITH-SCRIPT>
JWT_REFRESH_SECRET=<GENERATE-WITH-SCRIPT>

# ==================== JWT ====================

JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES_DAYS=7

# ==================== EMAIL (SMTP) ====================
# Configure with your email provider (Gmail, SendGrid, AWS SES, etc.)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-production-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM_EMAIL=noreply@yourdomain.com

# ==================== APP URLs ====================
# Use your production domain

APP_URL=https://yourdomain.com
SUPPORT_URL=https://yourdomain.com/support

# ==================== ADMIN SEED ====================
# Initial admin credentials (change after first login!)

ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=<STRONG-PASSWORD-CHANGE-AFTER-LOGIN>

# ==================== OPTIONAL: Logging ====================

LOG_LEVEL=info
LOG_FORMAT=json

# ==================== OPTIONAL: Rate Limiting ====================
# Uncomment and set if implementing rate limiting

# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# LOGIN_RATE_LIMIT_MAX=5
# REGISTER_RATE_LIMIT_MAX=3

# ==================== OPTIONAL: Security ====================

# HELMET_ENABLED=true
# CORS_ORIGIN=https://yourdomain.com

# ==================== OPTIONAL: Monitoring ====================

# SENTRY_DSN=https://...@sentry.io/...
# NEW_RELIC_LICENSE_KEY=...

# ==================== DEPLOYMENT CHECKLIST ====================
# 
# Before deploying to production:
# 
# ✅ Generate strong JWT_SECRET and JWT_REFRESH_SECRET
# ✅ Configure MongoDB connection string (use MongoDB Atlas or managed DB)
# ✅ Set up SMTP with production email provider
# ✅ Update APP_URL to production domain
# ✅ Change ADMIN_PASSWORD to a strong, unique password
# ✅ Enable HTTPS/SSL (via reverse proxy like Nginx)
# ✅ Set NODE_ENV=production
# ✅ Use PM2 or Docker for process management
# ✅ Configure backups for MongoDB
# ✅ Set up monitoring and alerting
# ✅ Enable rate limiting
# ✅ Configure helmet for security headers
# ✅ Test all flows (register, login, 2FA, password reset, email verification)
# ✅ Review docs/SECURITY.md before deployment
# ✅ Review docs/DEPLOYMENT.md for detailed instructions
# 
# See docs/DEPLOYMENT.md for complete deployment guide.
